Module basicMath

def a: 3;
def b: 5;
def c: a + b; // 8
def d: (a ^ b); // 243

def succ(x):
    x + 1;

def root(x, y):
    x^(1/y);

def sqrt(x):
    root(x, 2);

def add(x):
    x + succ(x);

2 * c;  // 16
b % 2;  // 1
add(8); // 17

// This language is case-insensitive regarding symbol names
Root(D, 3);  // 6.24..
Root(64, 3); // 4
Sqrt(81);    // 9
